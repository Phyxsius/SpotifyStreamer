<resources>

    <!-- Base application theme. -->
    <style name="SpotifyStreamer" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="PlayerTheme" parent="@style/Theme.AppCompat.Light.DialogWhenLarge">
    </style>

    <style name="SettingsTheme" parent="SpotifyStreamer">
    </style>

    <style name="GeneralListStyle">
        <!-- Here it's a stub.  In screens of sufficient width, this style includes modifications
       for two-pane layout -->
    </style>

</resources>
